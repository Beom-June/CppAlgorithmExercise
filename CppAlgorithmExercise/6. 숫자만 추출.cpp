#include<stdio.h>
#include<iostream>
using namespace std;

/*
int main()
{
	char a[100];
	int result = 0, count = 0, i = 0;
	cin >> a;

	for (i = 0; a[i] != '\0'; i++)
	{
		if (a[i] >= 48 && a[i] <= 57)
		{
			result = result * 10 + (a[i] - 48);
		}
	}
	cout << result << endl;

	for ( i = 1; i <= result; i++)
	{
		if (result % i == 0)
			count++;
	}
	cout << count << endl;

	return 0;
}
*/