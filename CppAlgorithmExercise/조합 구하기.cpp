#include<stdio.h>
#include<iostream>
#include<vector>
#include<queue>
#include<stack>
#include<algorithm>
using namespace std;

/*
int n, r;
int check[20];

void DFS(int s, int Level)
{
	if (Level == r)
	{
		for (int j = 0; j < Level; j++)
		{
			cout << check[j] << " ";
		}
		cout << endl;
	}
	else
	{
		for (int i = s; i < n; i++)
		{
			check[Level] = i;
			DFS(i + 1, Level + 1);
		}
	}
}

int main()
{

	cin >> n >> r;

	DFS(0, 0);

	return 0;
}
*/